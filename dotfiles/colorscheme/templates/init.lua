local alacritty_template = require("colorscheme.templates.alacritty")
local btop_template = require("colorscheme.templates.btop")
local dunst_template = require("colorscheme.templates.dunst")
local fish_template = require("colorscheme.templates.fish")
local imv_template = require("colorscheme.templates.imv")
local rofi_template = require("colorscheme.templates.rofi")
local sugarcandy_template = require("colorscheme.templates.sugarcandy")
local sway_template = require("colorscheme.templates.sway")
local swaylock_template = require("colorscheme.templates.swaylock")
local waybar_template = require("colorscheme.templates.waybar")
local zathura_template = require("colorscheme.templates.zathura")

return {
	alacritty = alacritty_template,
	btop = btop_template,
  dunst = dunst_template,
	fish = fish_template,
	imv = imv_template,
	rofi = rofi_template,
  sugarcandy = sugarcandy_template,
  sway = sway_template,
  swaylock = swaylock_template,
  waybar = waybar_template,
	zathura = zathura_template,
}
